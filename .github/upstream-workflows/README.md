# Upstream workflow references

These are the original Node.js CI definitions, retained for upstream comparison.
They are not enabled on this adaptation branch. Native HarmonyOS builds and
validation use the scripts in `tools/ohos`; the upstream hosted runner jobs do
not validate this target environment.
